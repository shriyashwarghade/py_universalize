class PyUniversalize:

    def __init__(self, config, primary, languages, display_name):
        self.config = config
        self.primary = primary
        self.languages = languages
        self.display_name = display_name
